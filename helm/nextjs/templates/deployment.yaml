apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nextjs-app.fullname" . }}
  labels:
    app: {{ include "nextjs-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "nextjs-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "nextjs-app.name" . }}
    spec:
      containers:
        - name: {{ include "nextjs-app.name" . }}
          image: "{{ .Values.image.domain }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 3000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
